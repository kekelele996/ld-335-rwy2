请根据以下要求生成项目代码和 README。

## 项目主要需求

**项目概述**：医保智能结算API网关——为医院信息系统提供标准化的医保结算接口服务，支持参保人身份核验、费用上传、预结算、正式结算和结算单查询，对接医保局接口规范。
**项目类型**：纯后端服务

### 功能需求

1. **参保人身份核验**：提供 RESTful API 接收身份证号和医保卡号，调用医保局接口或模拟接口验证参保人身份，返回参保状态（在职/退休/居民医保）、参保地、医保类型（职工/居民/新农合）、个人账户余额。

2. **费用明细上传**：接收医院上传的就诊费用明细（药品、诊疗、耗材、检查），每条明细包含项目编码、名称、单价、数量、金额、自付比例，系统进行数据格式校验和重复性检查，校验通过后生成上传批次号。

3. **预结算计算**：基于上传的费用明细和参保人信息，按医保目录（甲类、乙类、丙类）和参保地报销政策进行预结算，返回分项明细：总费用、医保报销金额、个人账户支付、自费金额、起付线、报销比例，支持多次预结算比对。

4. **正式结算与回退**：预结算确认后发起正式结算，生成唯一结算单号，结算成功后返回结算凭证信息，支持结算当日冲正（全额回退）和历史结算查询，所有结算记录持久化存储。

5. **结算单管理与对账**：提供结算单查询 API（按结算单号、参保人、日期范围），支持导出结算明细报表，提供日终对账接口（当日总笔数、总金额、成功/失败笔数），异常结算单标记待人工处理。

6. **API 文档与权限管理**：自动生成 OpenAPI/Swagger 接口文档，支持 API Key + JWT 双重认证，对不同调用方（ HIS 系统、第三方平台）分配不同的接口权限和调用频率限制，所有接口调用记录审计日志。

### 技术要求

- **英文短名（用于 Docker Compose）**：`gbinsureapi`
- **后端端口**：`BACKEND_PORT=19935`
- **后端框架**：FastAPI (Python 3.11)
- **数据库**：PostgreSQL
- **认证方式**：API Key + JWT
- **其他依赖**：SQLAlchemy（ORM）、Alembic（迁移）、Pydantic（数据校验）、Swagger UI（自动文档）

## 代码存放规则

后端代码放在 `backend/`：
API 路由、业务逻辑、数据模型、数据库脚本等放在对应的子文件夹

数据库脚本放在 `database/`。

## 部署要求

项目为纯后端服务，无前端页面，通过 API 接口提供服务。

需要提供以下文件：
- 根目录 `docker-compose.yml`
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbinsureapi`
- `backend/Dockerfile`
- `.env.example`

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbinsureapi`
- 所有服务使用 `container_name`，带 `${COMPOSE_PROJECT_NAME:-gbinsureapi}` 前缀
- 数据库数据使用命名卷持久化（顶层 `volumes:` 声明）
- 后端服务暴露端口 `19935`
- 数据库配置 `healthcheck`，后端 `depends_on` 数据库 healthy
- 支持 `docker compose up -d` 一条命令启动

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbinsureapi`
- `DB_NAME` / `DB_USER` / `DB_PASSWORD`
- `JWT_SECRET`
- `API_KEY_SECRET`
- `BACKEND_PORT=19935`

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 启动命令）
- API 访问地址和 Swagger 文档地址
- 技术栈
- 项目目录结构
- 环境变量说明
- API 调用示例（curl 或 Python requests）
- Docker 部署说明
- License

README 要求：
- Docker 部署命令放在最前面
- 本地开发方式作为备选
- 命令可直接复制运行
- 提供 API 调用示例代码

## Git 要求

代码编写完成后初始化 Git 并创建 `.gitignore`：

```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
__pycache__/
*.pyc
*.pyo
.venv/
```
